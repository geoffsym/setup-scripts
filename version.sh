#!/bin/bash

echo "> asdf --version"
asdf --version

echo "> python --version"
python --version

echo "> pip --version"
pip --version

echo "> node --version"
node --version

echo "> npm --version"
npm --version

echo "> docker --version"
docker --version